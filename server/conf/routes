# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           @controllers.ApplicationController.html(file="../client/dist/index.html")

# Entries
GET     /entries                    @controllers.EntryController.getEntries
POST    /entries                    @controllers.EntryController.addEntry

GET     /$file<.*\.(js|css)$>       controllers.ExternalAssets.at(path="../client/dist", file)
